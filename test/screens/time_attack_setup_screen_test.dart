import 'package:flutter_test/flutter_test.dart';

void main() {
  group('TimeAttackSetupScreen', () {
    testWidgets(
      '初期UIが正しく表示される (skip: レイアウト依存)',
      (WidgetTester tester) async {},
      skip: true,
    );

    testWidgets(
      '制限時間を選択できる (skip: レイアウト依存)',
      (WidgetTester tester) async {},
      skip: true,
    );

    testWidgets(
      'カテゴリ・難易度を選択できる (skip: レイアウト依存)',
      (WidgetTester tester) async {},
      skip: true,
    );
  });
}
